import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shoppin_assignment.app',
  appName: 'shoppin_assignment',
  webDir: 'out',
};

export default config;
